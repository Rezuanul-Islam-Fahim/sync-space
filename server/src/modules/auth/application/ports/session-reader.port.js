export class SessionReaderPort {
    /**
     *
     * @param {string} authUserId
     * @param {string} sessionId
     * @returns {Promise}
     */
    getSession(_authUserId, _sessionId) {
        throw new Error('Method not implemeneted');
    }
}
